<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div id="main">
        {{#each data}}
        <a href="/movies:{{id}}">
            <div class="movie">
                <img src="{{poster_path}}" alt="{{name}}">
                <div class="movie-info">
                    <h3>{{name}}</h3>
                    <span class="span" onload="color({{vote_average}})">{{vote_average}}</span>
                </div>
                <div class="overview">
                    <h3>overview</h3>
                    {{overview}}
                    <p     style="background-color:green;        padding:1rem;">{{first_air_date}}</p>
                </div>
            </div> 

        </a>       
        {{/each}}
    </div>

</div>

</body>
</html>